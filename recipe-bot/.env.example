# hello-agent와 동일한 키를 써도 됩니다:
#   cp ../hello-agent/.env .env
#
# 새로 발급할 경우: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional — 기본값은 gemini-2.5-flash. Vision 호환 모델이어야 함.
# GEMINI_MODEL=gemini-2.5-flash
